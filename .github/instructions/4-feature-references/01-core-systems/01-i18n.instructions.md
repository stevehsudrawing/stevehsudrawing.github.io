---
description: >
  Internationalization (i18n): multi-language support via useI18n() composable, $t() Vue plugin,
  and JSON translation files per language under public/configs/i18n/.
  i18n key conventions: text-* for plain text, html-* for rich HTML.
  Use when: adding translations, modifying useI18n.ts, i18n plugin, or language switching.
applyTo: >
  src/composables/useI18n.ts;
  src/plugins/i18n.ts;
  src/core/i18n.ts;
  public/configs/i18n/**
---

#### 4.1.1 I18n (Internationalization)

##### 4.1.1.1 Architecture

```
useI18n() composable         i18nPlugin (app.use)
  ├─ locale: Ref<Lang>         └─ provide(I18N_LOCALE_KEY)
  ├─ messages: Ref<...>           provide(I18N_MESSAGES_KEY)
  ├─ t(key, fallback?)            register $t() global
  ├─ setLocale(rawLang)          ─┐
  └─ initLang()                   │
                                  │
core/i18n.ts                      │
  ├─ langData: Record<...>        │
  ├─ currentLang: Lang            │
  ├─ applyLangData(lang, data) <-─┘
  └─ normalizeLang(raw)
```

> **Phase 7:** `syncFromLangData()`, `updatePageText()`, `AppEvent.PageTextUpdated`,
> `window.__navbar` reference, and the tooltip i18n listener were all removed —
> all tooltips now use `v-b-tooltip` from bootstrap-vue-next.

##### 4.1.1.2 i18n Key Naming

| Prefix   | Target        | Example                                        |
| -------- | ------------- | ---------------------------------------------- |
| `text-*` | `textContent` | `text-home`, `text-settings`                   |
| `html-*` | `innerHTML`   | `html-powered-by`, `html-steve-hsu-s-link-hub` |

> **Phase 7 note:** `hast-*` keys have been removed. The last remaining
> HAST key (`hast-powered-by`) was replaced by `html-powered-by` using
> `v-html="$t('html-powered-by')"` in FooterNav.vue.

##### 4.1.1.3 setLocale() Flow

1. `isLanguageLoading.value = true` (triggers LoadingBar via `watch`)
2. `fetch(/configs/i18n/{lang}.json)`
3. On success:
   - `locale.value = lang`
   - `messages.value = data` (triggers Vue re-render)
   - `applyLangData(lang, data)` (updates legacy page-title, theme UI)
   - `localStorage.setItem(StorageKey.Lang, lang)`
4. `isLanguageLoading.value = false`

##### 4.1.1.4 Language Switching via AppNavbar

AppNavbar dispatches `AppEvent.LangSwitchRequested` custom event, which is
handled by `useI18n()` `initLang()` listener -> `setLocale()`.

##### 4.1.1.5 Translation JSON Format

```json
{
  "text-home": "Home",
  "html-steve-hsu-s-link-hub": "<strong>Steve Hsu</strong>'s Link-Hub"
}
```

##### 4.1.1.6 Consumers

| File                           | How                                           |
| ------------------------------ | --------------------------------------------- |
| `App.vue`                      | `useI18n()` -> `initLang()`, `messages`       |
| `AppNavbar.vue`                | `useI18n()` -> `setLocale()`, `t()`, `locale` |
| `SettingsModal.vue`            | `useI18n()` -> `setLocale()`, `locale`        |
| `FooterNav.vue`                | `$t()` in template (no script import needed)  |
| `ResetWarningModal.vue`        | `useI18n()` -> `t()`                          |
| `LinkButtonGroup.vue`          | `useI18n()` -> `t()`                          |
| `ExternalLinkConfirmModal.vue` | `useI18n()` -> `t()`, `$t()`                  |
| `QRCodeModal.vue`              | `useI18n()` -> `t()`, `$t()`                  |
